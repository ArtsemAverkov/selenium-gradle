# 🚀 Selenium Java Automation Framework

Автоматизированный UI-фреймворк на **Java + Selenium**, спроектированный с учётом **реальных рабочих задач**, **CI/CD** и требований **технических собеседований**.

Проект демонстрирует не просто написание тестов, а **архитектуру автотестов, работу с окружениями и интеграцию с Jenkins**.

---

## 🧰 Стек технологий

- Java 11  
- Selenium WebDriver 4  
- TestNG  
- Gradle  
- WebDriverManager  
- Allure Reports  
- Jenkins (Declarative Pipeline)  
- Docker

---

## 🧠 Архитектура фреймворка

### 🔹 BaseTest

Базовый класс для всех тестов.

Отвечает за:
- инициализацию WebDriver
- корректное завершение сессии браузера
- подготовку окружения перед тестами

---

### 🔹 DriveFactory (Factory Pattern)

Централизованная фабрика для создания WebDriver.

Поддерживает:
- **LOCAL режим** — `ChromeDriver`
- **REMOTE режим** — `RemoteWebDriver` (Selenium Grid)

Режим запуска определяется через:
- JVM параметр: `-Drun.mode=local | remote`
- или переменную окружения `RUN_MODE`

Такой подход позволяет:
- запускать одни и те же тесты локально и в CI
- легко масштабировать фреймворк

---

### 🔹 YmlConfigReader

Класс для чтения настроек из `config.yml`.

Используется для:
- выбора браузера
- headless режима
- base URL
- централизованного управления конфигурацией

---

## ⚙️ Пример конфигурации (`config.yml`)

```yaml
browser: chrome
headless: false
baseUrl: https://example.com

 Запуск тестов
Локальный запуск (без Docker)
Запуск тестов в локальном Chrome:
./gradlew clean test -Drun.mode=local
Используется:
локально установленный браузер
WebDriverManager для драйверов

Удалённый запуск (Selenium Grid)
Запуск тестов в Docker-браузере:
./gradlew clean test \
-Drun.mode=remote \
-Dselenium.remote.url=http://localhost:4444/wd/hub
Используется:
RemoteWebDriver
браузер внутри Docker контейнера

Jenkins CI
В проекте реализован Declarative Jenkinsfile, который выполняет:
Checkout репозитория
Запуск автотестов
Генерацию Allure отчёта
Архивацию артефактов
Пайплайн может работать:
без Docker
с Selenium Grid
на любом Jenkins агенте с Java и Chrome

✅ Реализованные возможности
Factory Pattern для WebDriver
LOCAL / REMOTE режимы запуска
Конфигурация через YAML
Управление жизненным циклом тестов
Allure отчёты
Jenkins CI интеграция
Headless режим
CI-friendly архитектура


Планы развития
Page Object Model (POM)
Параллельный запуск тестов
Retry механизм
Логирование (SLF4J + Logback)
Поддержка нескольких браузеров
Docker Compose для Selenium Grid

👤 Автор
Artem Averkov
Automation QA Engineer (Java)
GitHub: https://github.com/ArtsemAverkov
